export interface ICreateUserDTO {
  email: string;
  firstName: string;
  lastName: string;
}

export interface IUserResponseDTO {
    email: string;
    firstName: string;
    lastName: string;
    // All other response
}